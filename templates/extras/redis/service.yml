services:
  redis:
    image: redis:alpine
    container_name: ${PROJECT_NAME}-redis
    networks:
      - devstack-internal
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10
