// Minimal Go module stub shipped by dev-strap so the agentic-dev preset
// builds out-of-the-box. Replace `module devstack-app` with your real
// module path once you have one (e.g. github.com/you/yourproject).
module devstack-app

go 1.25
